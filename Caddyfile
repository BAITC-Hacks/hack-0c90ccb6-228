{$SITE_ADDRESS} {
    encode zstd gzip
    request_body {
        max_size 8KB
    }
    reverse_proxy app:8000
    header {
        X-Content-Type-Options nosniff
        Referrer-Policy strict-origin-when-cross-origin
        -Server
    }
}
